using UnityEngine;
using System.Collections;


/** CameraBase should be applied to all cameras in scene.
 * It assigns control over the camera to either the game
 * cameras, which do minigame-specific stuff,
 * or retains its own control to do transitions to
 * the next scene.
 */
public class CameraBase : MonoBehaviour {


	// Use this for initialization
	void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {

	}
}
